// Rectangle
var rectangle = {
  length: 4,
  width: 4,
  isSquare: function(){
    if
      (this.length / this.width === 0)
      console.log("that is a square");
    else
      (console.log("that is not a square"));
  },
  area: function(){
    (result = this.length * this.width);
    return result;
  },
  perimeter: function(){
    result = (this.length * 2) + (this.width * 2);
    return result;
  }
};


 console.log(rectangle.isSquare);




// Triangle
var triangle = {
  sideA: 3,
  sideB: 4,
  sideC: 4
  isEquallateral: function(){
    if (sideA === sideB|| sideA === sideC || sideB)

  }



};
